build-Server:
	# The binary should be generated by the Makefile in the parent directory (make server)
	cp edav-server $(ARTIFACTS_DIR)/
build-ServerWithDomain:
	# The binary should be generated by the Makefile in the parent directory (make server)
	cp edav-server $(ARTIFACTS_DIR)/